function validateForm() {
    let couponName = document.querySelector('#coupon_name').value
    if (couponName === "")
        return false;

    let couponPrice = document.querySelector('#coupon_price').value
    if (couponPrice === "")
        return false;

    let

}